__pycache__/
*.pyc
.venv/
.env
.DS_Store
data/
# Run artefacts are local, except the control summary: grow/experiment.py points
# the agent at it for reference values, so it has to survive a fresh clone.
runs/*
!runs/control_summary.json

.autolab/
.playwright-mcp/
*.png
*.pdf
*.pptx
